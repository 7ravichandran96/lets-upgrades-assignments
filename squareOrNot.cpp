//entered values of length and breadth of a rectangle from user and check if it is square or not
#include<iostream>
using namespace std;
int main()
{
    float length,breadth;
    
    cout<<endl<<"enter the value of length = ";
    cin>>length;
    cout<<endl<<"enter the value of breadth = ";
    cin>>breadth;
    
    if (length == breadth)
    {
        cout<<endl<<"data enterd "<<length<<" and "<<breadth<<" are SQUARE";
        
    }
    if
    {
       cout<<endl<<"data enterd "<<length<<" and "<<breadth<<" are NOT A SQUARE ";

    }

    return 0;
};
